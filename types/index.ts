export type Job = {
  date: string;
  title: string;
  company: string;
  location: string;
  range: string;
  url: string;
  content: string[];
};

export type SKill = {
  name: string;
  icon: string;
};
